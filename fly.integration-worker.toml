app = "ngx-ramblers-integration-worker"
primary_region = "lhr"

[processes]
app = "npm run worker-server --prefix server"

[http_service]
auto_start_machines = true
auto_stop_machines = "suspend"
force_https = true
internal_port = 5001
min_machines_running = 0
processes = ["app"]

[[http_service.checks]]
interval = "15s"
timeout = "10s"
grace_period = "60s"
method = "GET"
path = "/api/health"

[[vm]]
memory = "2048mb"
cpu_kind = "performance"
cpus = 1
