export enum WalkImportFilterMatch {
  ALL = "all",
  MATCHED = "matched",
  UNMATCHED = "unmatched",
  TENTATIVE = "tentative",
  EXCLUDED = "excluded"
}

export enum WalkImportStepStatus {
  PENDING = "pending",
  ACTIVE = "active",
  DONE = "done"
}
