export enum WalkImportFilterMatch {
  ALL = "all",
  MATCHED = "matched",
  UNMATCHED = "unmatched",
  EXCLUDED = "excluded"
}

export enum WalkImportStepStatus {
  PENDING = "pending",
  ACTIVE = "active",
  DONE = "done"
}
