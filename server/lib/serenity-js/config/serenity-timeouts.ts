import { Duration } from "@serenity-js/core";

export const DEFAULT_WAIT_TIMEOUT = Duration.ofSeconds(90);

export const DEFAULT_INTERACTION_TIMEOUT = Duration.ofSeconds(45);

export const OS_MAPS_SCENARIO_TIMEOUT = Duration.ofSeconds(480);
